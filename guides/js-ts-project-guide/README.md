# JavaScript and TypeScript Project Guide

This guide is contributor onboarding material for developers who know Python and need to become productive in opencode's JavaScript and TypeScript codebase.

## Render

Render the PDF from this directory:

```bash
quarto render --to pdf
```

Quarto writes generated output to `_book/`. Do not commit generated PDFs, Quarto caches, or render-check images.
